# esphome-esp32-2432s028r
ESP32 2433S028R Configuration For ESPHOME

Copy `secrets.example.yaml` to `secrets.yaml` and fill in your API key, Wi-Fi credentials, and OTA password. The `.gitignore` keeps the secrets file out of version control to protect private values.
